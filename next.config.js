/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'res.cloudinary.com','www.twitterbio.io','github.com'],
    },
}

module.exports = nextConfig
